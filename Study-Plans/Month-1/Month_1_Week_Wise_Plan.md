# MONTH 1 — WEEK-WISE PLAN
### Java Fast-Track + DSA Foundation + HTML/CSS + Interview Layer

**Duration:** 5 weeks (Week 1 → Week 5)
**Daily commitment:** ~7 productive hours × 6 days/week
**Month goal:** Java OOP solid · 40 DSA problems · 20 classic programs · 55 interview questions · portfolio live · 1 timed coding test

> **Golden rule for this month:** the day you learn a topic, you write its interview questions. A topic is not "done" until its Q&A exists in `interview-prep/`.

---

## 📊 MONTH 1 AT A GLANCE

| Week | Java | DSA (problems) | HTML/CSS | Interview layer | Project |
|------|------|----------------|----------|-----------------|---------|
| **1** | Syntax → loops → patterns → methods | Complexity (5) + Arrays start (5) | HTML structure, semantic, forms, tables, media | Java Q1–8 · HTML/CSS Q1–6 · Programs 1–6 | — |
| **2** | Arrays 1D/2D, Strings, StringBuilder | Arrays finish (7) + 2D matrix (7) | CSS selectors, box model, units, display, position | Java Q9–14 · HTML/CSS Q7–13 · Programs 7–12 | Portfolio wireframe |
| **3** | OOP full (all 6 pillars + interfaces) | Prefix sum + Kadane (6) | Flexbox, Grid, responsive, media queries | Java Q15–24 · HTML/CSS Q14–20 · Programs 13–16 | Portfolio HTML+CSS |
| **4** | Exceptions, Collections, Comparable/Comparator, lambda | Strings (10) | Bootstrap (4h) + Tailwind (4h) | Java Q25–30 · HTML/CSS Q21–25 · Programs 17–20 | Portfolio polish |
| **5** | **Revision + gap filling** | Revision + weak-topic re-solve | Responsive fixes + deploy | **Full bank revision + 1 timed coding test** | **Deploy to GitHub Pages** |
| | **60 hrs** | **40 problems** | **~35 hrs** | **55 Q + 20 programs + 1 test** | **10 hrs** |

---

## 📚 MASTER RESOURCE LIST (bookmark these once, use all month)

### Java
| Resource | Use for | Link |
|----------|---------|------|
| **Apna College — Java + DSA (Hindi)** | Primary video course, matches your syllabus | https://www.youtube.com/playlist?list=PLfqMhTWNBTe3LtFWcvwpqTkUSlB32kJop |
| **Kunal Kushwaha — DSA + Java Bootcamp** | Deeper explanations, especially OOP & Collections | https://www.youtube.com/playlist?list=PL9gnSGHSqcnr_DxHsP7AW9ftq0AtAyYqJ |
| **W3Schools Java** | Quick syntax reference while coding | https://www.w3schools.com/java/ |
| **Programiz Java** | Clean examples with output | https://www.programiz.com/java-programming |
| **GeeksforGeeks Java** | Interview-oriented explanations | https://www.geeksforgeeks.org/java/ |
| **Oracle Java Tutorials** | Authoritative reference (use for OOP & Collections) | https://docs.oracle.com/javase/tutorial/ |
| **Java Collections cheat sheet (GfG)** | Collections comparison tables | https://www.geeksforgeeks.org/collections-in-java-2/ |

### DSA
| Resource | Use for | Link |
|----------|---------|------|
| **Striver A2Z DSA Sheet** | Your primary problem sheet — follow order | https://takeuforward.org/strivers-a2z-dsa-course/strivers-a2z-dsa-course-sheet-2/ |
| **NeetCode Roadmap** | Pattern-first visual roadmap | https://neetcode.io/roadmap |
| **LeetCode 75** | Curated interview-frequency problems | https://leetcode.com/studyplan/leetcode-75/ |
| **GfG Time Complexity** | Complexity theory + practice | https://www.geeksforgeeks.org/analysis-of-algorithms-set-1-asymptotic-analysis/ |
| **Big-O Cheat Sheet** | Quick reference for every DS operation | https://www.bigocheatsheet.com/ |

### HTML / CSS
| Resource | Use for | Link |
|----------|---------|------|
| **MDN HTML** | The reference. Not optional. | https://developer.mozilla.org/en-US/docs/Web/HTML |
| **MDN CSS** | The reference. | https://developer.mozilla.org/en-US/docs/Web/CSS |
| **freeCodeCamp Responsive Web Design** | Hands-on certification path | https://www.freecodecamp.org/learn/2022/responsive-web-design/ |
| **CSS-Tricks Flexbox Guide** | The best Flexbox explanation | https://css-tricks.com/snippets/css/a-guide-to-flexbox/ |
| **CSS-Tricks Grid Guide** | The best Grid explanation | https://css-tricks.com/snippets/css/complete-guide-grid/ |
| **Flexbox Froggy** | Game to lock in Flexbox (1 hr) | https://flexboxfroggy.com/ |
| **Grid Garden** | Game to lock in Grid (1 hr) | https://cssgridgarden.com/ |
| **Kevin Powell (YouTube)** | Best CSS teacher on the internet | https://www.youtube.com/kevinpowell |
| **Bootstrap 5 Docs** | Week 4 only — 4 hrs max | https://getbootstrap.com/docs/5.3/getting-started/introduction/ |
| **Tailwind Docs** | Week 4 only — 4 hrs max | https://tailwindcss.com/docs/utility-first |
| **GitHub Pages** | Deploying the portfolio | https://pages.github.com/ |

### Interview / Aptitude / English
| Resource | Use for | Link |
|----------|---------|------|
| **InterviewBit Java Questions** | Java Q&A bank source | https://www.interviewbit.com/java-interview-questions/ |
| **GfG Java Interview Questions** | Cross-check answers | https://www.geeksforgeeks.org/java-interview-questions/ |
| **MDN + freeCodeCamp HTML/CSS Q** | HTML/CSS Q&A bank source | https://www.freecodecamp.org/news/html-css-interview-questions/ |
| **IndiaBix Aptitude** | Daily 15 min aptitude | https://www.indiabix.com/aptitude/questions-and-answers/ |
| **BBC Learning English** | Grammar + listening | https://www.bbc.co.uk/learningenglish |
| **TED Talks** | Daily listening + speaking model | https://www.ted.com/talks |

---

## 🗓️ WEEK 1 — Java Foundations + Complexity + HTML Structure

**Theme:** Get fluent in writing Java, understand Big O, build correct HTML.

### Java (14 hrs)
| Day | Topic | Resource |
|-----|-------|----------|
| D1 | JDK/JRE/JVM, first program, compilation flow, `main` method | Apna College L1–2 · Oracle "Getting Started" |
| D1 | Variables, data types (primitive vs reference), type casting, literals | W3Schools Java Data Types |
| D2 | Operators (arithmetic, relational, logical, bitwise, ternary), precedence | Programiz Operators |
| D2 | Input/Output — `Scanner`, `System.out.printf` | W3Schools Java User Input |
| D3 | Conditionals: `if`/`else if`/`else`, nested if, `switch` (incl. enhanced switch) | Apna College L3 |
| D3 | Loops: `for`, `while`, `do-while`, `break`, `continue`, nested loops | Apna College L4 |
| D4 | **Patterns** — 10 star/number patterns (pyramid, inverted, diamond, Floyd, Pascal) | GfG "Pattern Programs in Java" |
| D5 | Methods: declaration, parameters, return types, **method overloading**, scope, recursion intro | Oracle "Defining Methods" |
| D6 | Practice day — rewrite 15 small programs without looking | — |

**Week 1 Java checkpoint:** you can write a menu-driven console program with methods, loops and switch without a reference.

### DSA — 10 problems (5 complexity + 5 arrays)
| # | Topic | Problems |
|---|-------|----------|
| 1–5 | **Time & space complexity** | Analyse 5 given code snippets: single loop, nested loop, halving loop, recursion, two sequential loops. Write Big O + Ω + Θ + best/avg/worst for each |
| 6–10 | **Arrays basics** | Traverse & print · find max & min · sum & average · count frequency of an element · linear search |

**Study first:** GfG Asymptotic Analysis → Big-O Cheat Sheet → Striver A2Z "Learn the Basics" section.

**Rule from day 1:** after every problem, write in your pattern notebook — *pattern name, trigger, time, space*.

### HTML (8 hrs)
- Document structure: `<!DOCTYPE>`, `html`, `head`, `body`, meta tags, title, favicon
- Text tags: headings, paragraph, `br`, `hr`, `strong`/`b`, `em`/`i`, `span`, `div`
- Links (`a`, target, relative vs absolute), images (`img`, `alt`, srcset basics)
- Lists (ul, ol, dl), tables (`table`, `thead`, `tbody`, `th`, `td`, colspan/rowspan)
- **Semantic HTML:** `header`, `nav`, `main`, `section`, `article`, `aside`, `footer`, `figure`
- Forms: `form`, `input` types (text, email, password, number, date, radio, checkbox, file), `label`, `select`, `textarea`, `button`, `required`, `placeholder`, `pattern`
- Media: `audio`, `video`, `iframe`
- Accessibility basics: `alt`, labels, heading order

**Resource:** MDN HTML → freeCodeCamp Responsive Web Design (HTML sections)
**Mini-task:** build a single-page HTML-only resume (no CSS) using only semantic tags + a form.

### 🎯 Interview layer — Week 1

**Java + OOP bank — Questions 1–8** (write question → 3-line answer → code snippet)
1. What is the difference between JDK, JRE and JVM?
2. Is Java platform independent, and why? What is bytecode?
3. What are the 8 primitive types and their sizes? Primitive vs reference type?
4. What is type casting? Implicit vs explicit, and where does data loss occur?
5. Difference between `==` and `.equals()` — with a String example.
6. Why are Strings immutable in Java? What is the String pool?
7. `String` vs `StringBuilder` vs `StringBuffer` — when to use which?
8. What is method overloading? Can you overload by return type alone? Why not?

**HTML/CSS bank — Questions 1–6**
1. What is semantic HTML and why does it matter (SEO + accessibility)?
2. Difference between block, inline and inline-block elements.
3. `div` vs `span` — when do you use each?
4. What does the `alt` attribute do and why is it required?
5. Difference between `id` and `class`.
6. What are the main HTML5 form input types and built-in validation attributes?

**Classic programs — 1 to 6** (write from memory, no IDE autocomplete, no Copilot)
1. Reverse a string
2. Check palindrome (string + number)
3. Fibonacci series (iterative **and** recursive)
4. Factorial (iterative + recursive)
5. Prime number check + print primes in a range
6. Armstrong number + perfect number

**Complexity Q&A:** for every DSA problem and every classic program, state time and space complexity out loud.

**Aptitude — 15 min/day:** Number system — divisibility rules, LCM/HCF, factors, remainders. *(IndiaBix → Aptitude → Numbers)*

**English — 30 min/day:**
- Grammar: tenses (present/past/future, all 4 forms each) — BBC Learning English
- Draft your **60-second self-introduction**; read it aloud 3× daily
- Listening: 1 short TED talk per day

### ✅ Week 1 exit check
- [ ] Can write loops, switch, methods and 10 patterns without reference
- [ ] 10 DSA problems done with complexity written for each
- [ ] HTML-only semantic resume page built
- [ ] Java Q1–8 + HTML/CSS Q1–6 written in `interview-prep/`
- [ ] Classic programs 1–6 written from memory
- [ ] Self-introduction drafted and rehearsed

---

## 🗓️ WEEK 2 — Arrays, Strings + CSS Fundamentals

**Theme:** Master array/string manipulation in Java; make HTML look like something.

### Java (14 hrs)
| Day | Topic | Resource |
|-----|-------|----------|
| D1 | 1D arrays: declaration, initialisation, traversal, `arr.length`, enhanced for | Apna College Arrays |
| D1 | Array operations: max/min, sum, reverse in-place, swap, copy, `Arrays.sort()`, `Arrays.toString()` | Oracle Arrays tutorial |
| D2 | **2D arrays**: declaration, row/column traversal, jagged arrays, matrix input/output | GfG Multidimensional Arrays |
| D2 | Matrix operations: transpose, addition, multiplication, diagonal sum | GfG Matrix programs |
| D3 | **String class**: creation, literal vs `new`, string pool, immutability | GfG Strings in Java |
| D3 | String methods: `length`, `charAt`, `substring`, `indexOf`, `contains`, `split`, `replace`, `trim`, `toUpperCase`, `equals` vs `equalsIgnoreCase`, `compareTo` | W3Schools String Methods |
| D4 | **StringBuilder**: `append`, `insert`, `delete`, `reverse`, `setCharAt`; why it beats `String` in loops | Oracle StringBuilder |
| D4 | Character class: `isDigit`, `isLetter`, `toUpperCase`, ASCII arithmetic | GfG Character class |
| D5–D6 | Practice: 20 array + string programs | — |

### DSA — 14 problems (7 arrays + 7 matrix)
**Arrays (7)** — building on Week 1
- Reverse an array in place
- Rotate array by k (left and right, using reversal algorithm)
- Second largest element (single pass)
- Remove duplicates from a sorted array
- Move all zeros to end
- Check if array is sorted / rotated sorted
- Union & intersection of two arrays

**2D arrays / Matrix (7)**
- Print matrix + row-wise / column-wise sum
- Transpose a matrix
- Rotate matrix by 90°
- **Spiral matrix traversal**
- Search in a row-and-column-sorted matrix
- Diagonal sums
- Set matrix zeroes

**Sheet reference:** Striver A2Z → "Arrays [Easy]" + "Arrays [Medium]" first half.

### CSS (10 hrs)
- CSS syntax, how to include (inline/internal/external), cascade & inheritance
- **Selectors:** element, class, id, group, descendant, child, attribute, pseudo-class (`:hover`, `:focus`, `:nth-child`), pseudo-element (`::before`, `::after`)
- **Specificity** and how conflicts are resolved; `!important` (and why to avoid it)
- Colours (hex, rgb, rgba, hsl), fonts, `font-family` stacks, Google Fonts, text properties
- **Box model:** content, padding, border, margin, `box-sizing: border-box`, margin collapse
- **Units:** px, %, em, rem, vh, vw, ch, fr — when to use each
- `display`: block, inline, inline-block, none
- `position`: static, relative, absolute, fixed, sticky + `z-index`
- Backgrounds, borders, border-radius, box-shadow, opacity

**Resource:** MDN CSS → Kevin Powell "CSS basics" videos → freeCodeCamp CSS sections
**Mini-task:** style your Week-1 HTML resume — typography, colours, spacing, a card layout using only box model + position.

### 🎯 Interview layer — Week 2

**Java bank — Questions 9–14**
9. How are arrays stored in memory? Array vs ArrayList.
10. Why is array size fixed in Java? What happens on index out of bounds?
11. Explain `String s = "abc"` vs `String s = new String("abc")` — how many objects are created?
12. What does `intern()` do? How does the String constant pool work?
13. Is Java pass-by-value or pass-by-reference? Prove it with an example (primitive + object).
14. What is the difference between `length` (array), `length()` (String) and `size()` (Collection)?

**HTML/CSS bank — Questions 7–13**
7. Explain the CSS box model. What does `box-sizing: border-box` change?
8. `em` vs `rem` vs `px` vs `%` — when do you use each?
9. Explain CSS specificity. Calculate specificity for a given selector.
10. All `position` values explained — with a use case for each.
11. `visibility: hidden` vs `display: none` vs `opacity: 0`.
12. Pseudo-class vs pseudo-element — give examples.
13. What is the cascade? How is inheritance different from the cascade?

**Classic programs — 7 to 12**
7. GCD and LCM (Euclidean algorithm)
8. Swap two numbers without a temp variable
9. Count vowels, consonants, digits and spaces in a string
10. Remove duplicates from an array
11. Second largest element in an array
12. Sum of digits + reverse a number

**Aptitude — 15 min/day:** Percentages, profit & loss basics.

**English — 30 min/day:**
- Grammar: articles (a/an/the), prepositions, subject–verb agreement
- **Record** your 60-second self-introduction and listen back once
- Vocabulary: 10 technical words/day (compile, deploy, iterate, immutable, invoke, instantiate, override, inherit, encapsulate, refactor…)

### ✅ Week 2 exit check
- [ ] 24 DSA problems total, complexity written for each
- [ ] Can solve any array/matrix manipulation problem in Java without help
- [ ] Resume page fully styled with box model, position, typography
- [ ] Java Q1–14 + HTML/CSS Q1–13 in `interview-prep/`
- [ ] Classic programs 1–12 written from memory
- [ ] Self-introduction recorded

---

## 🗓️ WEEK 3 — Java OOP (the most important week) + Flexbox/Grid

**Theme:** OOP is the single most-asked topic in every interview you will face. Do not rush this week.

### Java OOP (16 hrs)
| Day | Topic | Resource |
|-----|-------|----------|
| D1 | Class & object, fields vs methods, object creation, `new`, memory (stack vs heap) | Kunal Kushwaha OOP Part 1 |
| D1 | **Constructors:** default, parameterised, copy constructor, constructor overloading, `this()` chaining | Oracle "Providing Constructors" |
| D2 | `this` keyword (all 4 uses), `static` variables/methods/blocks, `final` (variable/method/class) | GfG static & final |
| D2 | **Encapsulation:** private fields, getters/setters, why it matters | Oracle Encapsulation |
| D3 | **Inheritance:** `extends`, single/multilevel/hierarchical, `super` (field/method/constructor), why no multiple inheritance | Kunal Kushwaha Inheritance |
| D3 | Method overriding rules, `@Override`, covariant return types | GfG Overriding |
| D4 | **Polymorphism:** compile-time (overloading) vs runtime (overriding), dynamic method dispatch, upcasting/downcasting, `instanceof` | Oracle Polymorphism |
| D4 | **Abstraction:** `abstract` class, abstract methods, when to use | GfG Abstract class |
| D5 | **Interfaces:** declaration, `implements`, multiple interfaces, default & static methods, interface vs abstract class | Oracle Interfaces |
| D5 | **Access modifiers:** private, default, protected, public — full access table; packages & `import` | GfG Access Modifiers |
| D6 | `Object` class basics: `toString`, `equals`, `hashCode` (contract), `getClass` | GfG equals & hashCode |
| D6 | **Practice:** model 5 real systems in OOP — BankAccount, Library, Employee hierarchy, Shape (abstract), Vehicle (interface) | — |

**Week 3 Java checkpoint:** you can design a 4-class system with inheritance, an interface and an abstract class, from a verbal requirement, without notes.

### DSA — 6 problems (Prefix sum + Kadane)
- Build a prefix sum array; answer range-sum queries in O(1)
- Equilibrium index
- Subarray sum equals K (prefix sum + HashMap)
- **Kadane's algorithm** — maximum subarray sum
- Maximum subarray sum with the actual subarray printed
- 2D prefix sum — sub-matrix sum query

**Study first:** Striver A2Z → "Arrays [Medium]" → prefix sum & Kadane articles.

### CSS Layout (10 hrs)
- **Flexbox:** `display:flex`, `flex-direction`, `justify-content`, `align-items`, `align-content`, `flex-wrap`, `gap`, `flex-grow`/`shrink`/`basis`, `align-self`, `order`
- **Grid:** `display:grid`, `grid-template-columns/rows`, `fr`, `repeat()`, `minmax()`, `auto-fit`/`auto-fill`, `gap`, `grid-template-areas`, item placement, alignment
- **Responsive design:** mobile-first approach, media queries, breakpoints, fluid typography, responsive images
- Common layouts to build by hand: responsive navbar, card grid, hero section, footer, sidebar layout, centred modal

**Resources:** CSS-Tricks Flexbox Guide → **Flexbox Froggy** (finish all 24 levels) → CSS-Tricks Grid Guide → **Grid Garden** (finish all 28 levels) → Kevin Powell responsive design videos

**Project work (4 hrs):** build the **portfolio layout** — header/nav, hero, about, skills grid, projects card grid, contact form, footer. Mobile-first, using Flexbox + Grid.

### 🎯 Interview layer — Week 3 (OOP-heavy)

**Java bank — Questions 15–24**
15. What are the 4 pillars of OOP? Give a code example for each.
16. Constructor vs method — 5 differences. Can a constructor be `final`/`static`/`abstract`?
17. What is constructor chaining? Difference between `this()` and `super()`.
18. Method overloading vs method overriding — full comparison table.
19. What is runtime polymorphism / dynamic method dispatch? How does the JVM resolve it?
20. Abstract class vs interface — when do you choose which?
21. Can an interface have a constructor? Can it have method bodies? (default/static/private methods)
22. Why does Java not support multiple inheritance for classes? How do interfaces solve it?
23. Explain all 4 access modifiers with an access table (same class / same package / subclass / world).
24. What are `static` blocks, `static` variables and `static` methods? Can `static` methods be overridden?

**Bonus (write them, they get asked):** `equals()` and `hashCode()` contract · composition vs inheritance · SOLID principles in one line each.

**HTML/CSS bank — Questions 14–20**
14. Flexbox vs Grid — when do you use which?
15. Explain `justify-content` vs `align-items` (and how the axes flip with `flex-direction`).
16. What does `flex: 1` actually mean (grow/shrink/basis)?
17. What are media queries? What is mobile-first and why is it preferred?
18. How do you centre a div — 4 different ways.
19. What is `fr` in Grid? Difference between `auto-fit` and `auto-fill`.
20. What makes a site responsive? (viewport meta, fluid units, media queries, flexible images)

**Classic programs — 13 to 16**
13. Matrix transpose
14. Matrix addition and multiplication
15. Bubble sort (by hand, with swap count)
16. Selection sort (by hand)

**Aptitude — 15 min/day:** Ratio & proportion, averages.

**English — 30 min/day:**
- Explain **one OOP concept aloud in English every day** (5 concepts this week) — record 2 of them
- Grammar: sentence structure, active vs passive voice
- Vocabulary: 10 words/day

### ✅ Week 3 exit check
- [ ] 30 DSA problems total
- [ ] Can design a 4-class OOP system verbally, then code it
- [ ] Flexbox Froggy + Grid Garden completed
- [ ] Portfolio layout built and responsive
- [ ] Java Q1–24 + HTML/CSS Q1–20 in `interview-prep/`
- [ ] Classic programs 1–16 written from memory
- [ ] Explained 5 OOP concepts aloud in English

---

## 🗓️ WEEK 4 — Exceptions, Collections + Bootstrap/Tailwind + Strings DSA

**Theme:** Collections are your DSA toolbox. Exceptions are guaranteed interview questions.

### Java (16 hrs)
| Day | Topic | Resource |
|-----|-------|----------|
| D1 | **Exception handling:** error vs exception, checked vs unchecked, `try-catch-finally`, multi-catch, nested try | Oracle Exceptions trail |
| D1 | `throw` vs `throws`, custom exceptions, try-with-resources (basic), exception hierarchy | GfG Exceptions in Java |
| D2 | **Collections overview:** the hierarchy (Collection → List/Set/Queue, plus Map) | GfG Collections in Java |
| D2 | `ArrayList` (internal growth), `LinkedList`, when to use which | Oracle List interface |
| D3 | `Stack`, `Queue`, `Deque` (`ArrayDeque`), `PriorityQueue` (min & max heap) | GfG Deque & PriorityQueue |
| D4 | `HashMap` (**internal working — buckets, hashing, collision, treeify**), `LinkedHashMap`, `TreeMap` | GfG HashMap internal working |
| D4 | `HashSet`, `LinkedHashSet`, `TreeSet` | GfG Set interface |
| D5 | **Comparable vs Comparator**, custom sorting, `Collections.sort`, `List.sort` | Oracle Object Ordering |
| D5 | **Lambda syntax — only for comparators**: `(a,b) -> a.x - b.x`, method references in sorting | GfG Lambda Expressions (read only the comparator part) |
| D6 | Iterators, `for-each` over collections, `ConcurrentModificationException` | GfG Iterators |

> **Do NOT go further:** no Stream API deep dive, no File I/O, no Serialization, no generics theory, no functional interfaces beyond comparators.

### DSA — 10 problems (Strings)
- Reverse a string / reverse words in a sentence
- Check palindrome (ignoring case & non-alphanumeric)
- Valid anagram (two approaches: sort + frequency map)
- First non-repeating character
- Character frequency count / most frequent character
- Longest common prefix
- String compression (run-length encoding)
- Check if two strings are rotations of each other
- Count and remove duplicate characters
- Implement `atoi` / string-to-integer with edge cases

**Sheet reference:** Striver A2Z → "Strings [Easy]".

### CSS Frameworks (8 hrs total — strictly time-boxed)
**Bootstrap 5 — 4 hrs max**
- Setup via CDN, container/row/col grid system, breakpoints
- Components you will actually use: navbar, card, button, form, modal, alert
- Utility classes: spacing, colours, display, flex helpers
- *Resource:* Bootstrap 5 docs → "Layout" and "Components"

**Tailwind CSS — 4 hrs max**
- Utility-first philosophy, why it exists
- Spacing/sizing/colour/typography utilities, responsive prefixes (`md:`, `lg:`), hover/focus variants
- Flex & Grid utilities
- *Resource:* Tailwind docs → "Utility-First Fundamentals" + "Responsive Design"

**Deliverable:** rebuild **one section** of your portfolio twice — once in Bootstrap, once in Tailwind — so you can talk about both in an interview. Keep whichever you prefer for the final site.

### Project — Portfolio polish (4 hrs)
- Add a bit of vanilla JS: mobile menu toggle, smooth scroll, simple form validation
- Cross-browser check, responsive check at 320 / 768 / 1024 / 1440 px
- Accessibility pass: alt text, label-for, heading order, colour contrast
- Lighthouse audit — aim for 90+ on Accessibility and Best Practices

### 🎯 Interview layer — Week 4

**Java bank — Questions 25–30** (completes the 30)
25. Checked vs unchecked exceptions — with examples of each.
26. `throw` vs `throws` vs `throwable`. Can `finally` be skipped? What is `finally` vs `finalize()`?
27. `ArrayList` vs `LinkedList` — internal structure, time complexity, when to use which.
28. **How does HashMap work internally?** (hashing → bucket → collision → linked list → treeify at 8, load factor 0.75, resize)
29. `HashMap` vs `HashSet` vs `TreeMap` vs `LinkedHashMap` — comparison table.
30. `Comparable` vs `Comparator` — with code for both, and when you'd use each.

**HTML/CSS bank — Questions 21–25** (completes the 25)
21. What is the CSS `!important` rule and why should it be avoided?
22. What are CSS variables (custom properties) and how do you use them?
23. How does the browser render a page? (HTML parse → DOM → CSSOM → render tree → layout → paint)
24. What causes reflow vs repaint? How do you avoid layout thrashing?
25. Bootstrap vs Tailwind — what is the difference in approach, and which would you choose for a project and why?

**Classic programs — 17 to 20** (completes the 20)
17. Insertion sort (by hand)
18. Linear search vs binary search (iterative + recursive)
19. Anagram check (both approaches)
20. Star patterns — the full set of 10, from memory

**Aptitude — 15 min/day:** Number series, simple interest, time & work intro.

**English — 30 min/day:**
- Explain your **portfolio project in English for 3 minutes**, recorded
- Grammar: common mistakes and corrections
- Listening: 1 tech talk daily, note 5 new words

### ✅ Week 4 exit check
- [ ] 40 DSA problems total — Month 1 DSA target hit
- [ ] All Collections used at least once in real code
- [ ] Can explain HashMap internal working on a whiteboard
- [ ] Portfolio complete, responsive, accessible
- [ ] **All 30 Java + 25 HTML/CSS questions written and answered**
- [ ] **All 20 classic programs written from memory**
- [ ] Portfolio explained aloud in English for 3 minutes

---

## 🗓️ WEEK 5 — Revision, Deployment, Timed Test & Month Gate

**Theme:** No new topics. Consolidate, prove it, and ship.

### Day-by-day

**Day 1 — Java revision (7 hrs)**
- Re-write, from memory: one class using inheritance + interface + abstract class + exception handling + a Collection + a custom Comparator
- Re-read all 30 Java questions; mark every one you could not answer cold
- Re-attempt the 5 weakest classic programs

**Day 2 — DSA revision (7 hrs)**
- Re-solve 10 problems you originally needed help with (from the mistake notebook)
- Re-derive complexity for all 40 problems — say it out loud
- Consolidate the **pattern notebook**: one page each for Arrays, Prefix Sum, Kadane, Matrix Traversal, String Frequency, Two-Pointer intro

**Day 3 — HTML/CSS revision + portfolio finalisation (7 hrs)**
- Re-read all 25 HTML/CSS questions
- Final responsive pass + Lighthouse re-audit
- Write the portfolio **README**: what it is, tech used, how to run, live link, screenshot

**Day 4 — 🚀 DEPLOY + Git (7 hrs)**
- Git essentials: `init`, `add`, `commit`, `branch`, `push`, `.gitignore`, meaningful commit messages
- Push portfolio to GitHub, enable **GitHub Pages**, verify the live URL
- Polish the GitHub profile: bio, profile README, pin the repo
- *Resource:* https://pages.github.com/ and https://docs.github.com/en/get-started

**Day 5 — 🧪 TIMED CODING TEST (the real gate)**
- **60 minutes, 2 problems, no IDE hints, no Copilot, no searching**
- Suggested: 1 easy array problem + 1 easy/medium string problem on LeetCode or HackerRank
- Immediately after: write down what went wrong, what you forgot, how long each took
- Then: solve both again cleanly and add both to the pattern notebook

**Day 6 — Month 1 gate + Month 2 setup (7 hrs)**
- Self-quiz: 20 random questions pulled from the 55-question bank, answered aloud and timed
- Run the full **Month 1 exit check** below
- Log everything you failed into the mistake notebook — these become Week-1 revision items in Month 2
- Set up Month 2: install Node + VS Code extensions, create the React project folder, bookmark JS/React resources

**Aptitude this week:** 1 sectional test (25 questions, 25 minutes) covering number system + percentages + ratio + series.

**English this week:**
- Final polished recording of the 60-second self-introduction
- 5-minute unscripted explanation of *"what I learned in Month 1"*

---

## ✅ MONTH 1 EXIT CHECK (from the master roadmap)

- [ ] Can write any Java OOP class without reference
- [ ] 40 DSA problems solved, complexity stated for each
- [ ] 20 classic interview programs written from memory
- [ ] Java/OOP (30) + HTML/CSS (25) question banks answered
- [ ] 1 timed coding test completed and reviewed
- [ ] Portfolio live on GitHub Pages
- [ ] Can explain arrays/strings approach verbally in English

### Additional Month-1 evidence to have in the repo
- [ ] `interview-prep/01-java-oop.md` — 30 Q&A
- [ ] `interview-prep/02-html-css.md` — 25 Q&A
- [ ] `classic-programs/` — 20 programs, each with complexity in a comment
- [ ] `dsa/month-1/` — 40 solved problems with approach notes
- [ ] `notebooks/pattern-notebook.md` — 6+ patterns documented
- [ ] `notebooks/mistake-notebook.md` — every failure logged
- [ ] Portfolio repo with README + live GitHub Pages link
- [ ] Green GitHub contribution graph for the whole month

---

## ⏱️ DAILY TEMPLATE FOR MONTH 1

```
08:30 – 09:00   Revise yesterday (DSA + Java) + plan today
09:00 – 11:00   DSA — learn the topic + solve
11:00 – 11:15   Break
11:15 – 13:00   DSA practice + classic interview programs
13:00 – 14:00   Lunch + rest
14:00 – 15:30   Java learning (per the week's table)
15:30 – 16:00   Break
16:00 – 17:30   HTML/CSS + portfolio hands-on
17:30 – 18:00   Break
18:00 – 18:45   🎯 INTERVIEW BLOCK — write this week's Q&A, answer aloud
18:45 – 19:00   Aptitude (15 min)
19:00 – 19:30   English (grammar / speaking / listening)
19:30 – 20:00   Mistake notebook + GitHub commit
```

**Weekly rhythm:** Mon–Fri as above · **Saturday** = portfolio + weak-topic clearing · **Sunday** = weekly revision + a timed mini-test + plan next week.

---

## 🚫 MONTH 1 — WHAT NOT TO DO

1. Do **not** study Stream API, File I/O, Serialization, generics theory or threads.
2. Do **not** spend more than 4 hrs each on Bootstrap and Tailwind.
3. Do **not** touch JavaScript frameworks — vanilla JS only, and only a little.
4. Do **not** postpone the interview block "until you finish learning". It runs from Week 1.
5. Do **not** use Copilot or AI to solve DSA problems or write the classic programs this month. Build the muscle first.
6. Do **not** exceed 40 DSA problems chasing volume — depth and complexity analysis matter more.
7. Do **not** over-polish the portfolio. 10 hours, ship it, move on.

---

*Month 1 of the 6-Month Interview-Ready Roadmap — AI Full Stack Developer*
*Targets: 60 hrs Java · 40 DSA problems · 20 classic programs · 55 interview questions · 1 timed test · 1 deployed project*
